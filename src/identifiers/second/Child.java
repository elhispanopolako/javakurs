package identifiers.second;

import identifiers.first.Base;

public class Child extends Base {
    public void testidentyfier() {
        System.out.println(first);
        System.out.println(third);
        firstmethod();
        thirdmethod();
    }
}